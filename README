= C extension
Utilrb includes a C extension in ext/. It is optional, but some of the functionalities
will be disabled if it is not present

* some methods have both a Ruby version and a C version (for performance):
  - Enumerable#each_uniq
* some methods have only a C version
  - ValueSet class


